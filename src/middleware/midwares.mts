import styles from 'ansi-styles';
import express, {Express, Request, Response} from "express";

/**
 * 
 * @param req 
 * @param res 
 * @param mes 
 * @param tab 
 */
export function log_actions(req: Request, res: Response, mes: string = "", tab: number = 0): void{

}